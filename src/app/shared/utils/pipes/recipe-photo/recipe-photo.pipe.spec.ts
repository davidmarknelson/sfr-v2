import { RecipePhotoPipe } from './recipe-photo.pipe';

describe('RecipePhotoPipe', () => {
  it('create an instance', () => {
    const pipe = new RecipePhotoPipe();
    expect(pipe).toBeTruthy();
  });
});
